# Enable transparent reverse proxy (browser keeps using your IP/host)
PROXY_MODE=1

# Upstream target (must include scheme, no trailing path)
UPSTREAM_BASE_URL=http://182.92.3.138/

# Optional: rewrite upstream Set-Cookie Domain=<upstreamHost> into host-only cookie
# Useful when users access this service via IP and upstream sets Domain=example.com
PROXY_REWRITE_COOKIE_DOMAIN=0


